import sqlc/lib/internal/sqlc

pub fn build(sqlc: sqlc.SQLC) {
  todo
  // for each of these, go through the queries + generate input, output types + build in the details
  // we'll also be wrapping these types w/ conn from sqlight because it's what we do. 
}
